import React from 'react';

//functional component in the mean time, if needed chang into a class component or even a container
const LoginPage = () => (
    <div>login page here</div>
);

export default LoginPage;